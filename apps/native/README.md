# TasteTrail Native

The native client is intentionally scaffolded as a first-class app surface in the Vercel + Neon rearchitecture.

It is not included in the root workspace install/build to keep the core web + CLI development loop lightweight.

Local run:

```bash
cd apps/native
npm install
npm run dev
```
